- if organization.commits.blank?
  ul data-orbit=true
    - organization.users.each do |user|
      li
        = image_tag user.avatar_url
        .orbit-caption
          = user.login
- else
  = render 'employees_li', employee: employee, index: index
